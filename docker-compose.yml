version: '3.8'

services:
  gpt-auto-manager:
    build:
      context: .
      dockerfile: Dockerfile
    image: gpt-auto-manager:latest
    container_name: gpt-auto-manager
    restart: unless-stopped
    ports:
      - "127.0.0.1:18000:8000"
    environment:
      - HOST_PROJECT_PATH=${PWD}
      - AUTO_UPDATE_HELPER_IMAGE=gpt-auto-manager:latest
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
      # 如果你要启用 Clash 订阅自助更新，再打开下面 3 条挂载：
      # - /opt/mihomo-pool:/opt/mihomo-pool
      # - /usr/bin/docker:/usr/local/bin/docker:ro
